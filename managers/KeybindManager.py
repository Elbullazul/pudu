class KeyListener():
	gdkListener = ''		# GTK keybind listener to show apps
	
	def registerKeybind(window, key, mask, method):
		# accel = Gtk.AccelGroup()
		# gdkListener.connect(key, mask, method)
		# window.add_accel_group(accel)
		
		return 	# trigger (something that the app can bind an action to)
